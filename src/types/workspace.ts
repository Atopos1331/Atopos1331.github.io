export type LayoutMode = "split" | "terminal" | "preview";

export type PreviewMode = "normal" | "forced";

export type WorkspaceBackgroundPath = string;

// Background blur strength in pixels (0 = none).
export type WorkspaceBackgroundBlur = number;
